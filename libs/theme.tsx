import { extendTheme } from '@chakra-ui/react';

const fonts = {
  heading: "'Indira K', sans-serif",
};

const colors = {};

const styles = {};

const components = {};

const config = {
  initialColorMode: 'dark',
};

const theme = extendTheme({ config, styles, components, fonts, colors });
export default theme;
